#include <stdio.h>

char main() 
{
  char kar;
  int i =
  for ( i = 0; i < count; i++)
  {
    /* code */
  }
  
  {
    jumkar = jumkar + 1;
    if (kar == ' ')
    	jumspasi = jumspasi + 1;
  }
  printf("\nJumlah karakter = %d", jumkar);
  printf("\nJumlah SPASI - %d\n\n", jumspasi);
}